{
  "compilerOptions": {
    "target": "ES6", // Specifies the target JavaScript version. ES6 is a common choice for modern projects.
    "module": "ESNext", // Specifies the module code generation method. ESNext allows for the latest module features.
    "lib": ["DOM", "ESNext"], // Includes type definitions for the DOM and the latest ECMAScript features.
    "allowJs": true, // Allows JavaScript files to be compiled.
    "jsx": "react-jsx", // Specifies the JSX code generation mode (React 17 and above).
    "strict": true, // Enables strict type-checking options.
    "esModuleInterop": true, // Enables emit interoperability between CommonJS and ES Modules.
    "skipLibCheck": true, // Skips type checking of declaration files.
    "forceConsistentCasingInFileNames": true, // Ensures that file names are consistent across the project.
    "baseUrl": "./", // Base directory for resolving non-relative module names.
    "paths": {
      "@/*": ["src/*"] // Maps import paths, useful for absolute imports.
    },
    "resolveJsonModule": true, // Allows importing JSON files.
    "noEmitOnError": true, // Prevents emitting output files if there are errors.
    "experimentalDecorators": true, // Enables experimental support for decorators.
    "emitDecoratorMetadata": true // Emits design-type metadata for decorators.
  },
  "include": [
    "src" // Specifies the files and directories to be included in the compilation.
  ],
  "exclude": [
    "node_modules", // Specifies the files and directories to be excluded from the compilation.
    "build" // Optionally exclude build output directories.
  ]
}
